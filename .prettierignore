# Build output / generated
dist/
coverage/
node_modules/
*.tsbuildinfo
package-lock.json

# Legacy Python tree (never touched / reformatted)
talk_codebase/
*.py

# Legacy plugin (kept as reference per never-delete; not reformatted)
plugin/legacy/
# Vendored / bundled assets we don't reflow
plugin/icons/

# Test fixtures with exact byte content
test/fixtures/

# Docs and config we don't want reflowed
*.md

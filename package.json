{
  "name": "chatgml",
  "version": "0.1.0",
  "description": "GameMaker-aware agentic coding assistant over any OpenAI-compatible backend.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "chatgml": "dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "dev": "tsx src/cli.ts",
    "ci": "npm run typecheck && npm run build && npm run coverage"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "ignore": "^7.0.5",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.20.3",
    "typescript": "~5.7.3",
    "vitest": "^3.2.4"
  }
}
